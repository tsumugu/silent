/**
 * Zandle - Zustand + IPC Synchronization Layer (Renderer)
 * Export barrel for middleware and API helpers
 */

export { zandle } from './middleware';
export type { ZandleAPI } from './api';
export { getZandleAPI } from './api';
